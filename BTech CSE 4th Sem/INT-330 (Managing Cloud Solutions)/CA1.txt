consider a scenario you have built a vpc with cidr 10.0.0.0/16, spanning 2 availabilty zones ( a and b) each az has one public subnet and one private subnet, an ig is atached to the vpc for public internet access, a nat gateway is deployed in the public subnet of az-a, a route table are configured so that public subnet route 0.0.0.0/0 traffic to the nat gateway. a web server is deployed in the public subnet route 0.0.0.0/0 traffic to the NAT gateway. a web server is deployed in the public subnet of az-b and a database server is deployed in the privaet subnet of az-b. implement in sandbox
